# HOME ASSISTANT automation, NOT ESPHome YAML.
# Replace entity_id with the ACTUAL entity ID from your Home Assistant.
# This uses the event.received syntax in the current HA event documentation.
# Disable the old MQTT automation during a switch-over to avoid duplicate actions.
alias: RFLink API – 085372 távirányító 08 gomb teszt
triggers:
  - trigger: event.received
    target:
      entity_id: event.rflink_esphome_rf_085372_taviranyito
    options:
      event_type:
        - button_08
conditions: []
actions:
  - action: persistent_notification.create
    data:
      notification_id: rflink_api_test
      title: RFLink API
      message: Megérkezett az 085372 távirányító 08 gombjának jele.
mode: single
